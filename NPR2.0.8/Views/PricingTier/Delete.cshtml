@model NPR2._0._8.Models.PricingTier

@{
    ViewBag.Title = "Delete Pricing Tier";
}

<h2>Delete Pricing Tier</h2>

<h3>Are you sure you want to delete this Pricing Tier?</h3>
<fieldset>
    <legend>PricingTier</legend>

    <div class="display-label">
        @Html.DisplayNameFor(model => model.PricingTierName)
    </div>
    <div class="display-field">
        @Html.DisplayFor(model => model.PricingTierName)
    </div>

    <div class="display-label">
        @Html.DisplayNameFor(model => model.PricingTierLevel)
    </div>
    <div class="display-field">
        @Html.DisplayFor(model => model.PricingTierLevel)
    </div>

    <div class="display-label">
        @Html.DisplayNameFor(model => model.Company.CompanyName)
    </div>
    <div class="display-field">
        @Html.DisplayFor(model => model.Company.CompanyName)
    </div>
</fieldset>
@using(Html.BeginForm(new { ReturnUrl = ViewBag.ReturnUrl }))
{
    @Html.AntiForgeryToken()
    <p>
        <input type="submit" value="Delete" />
        |
        @Html.ActionLink("Back to List", "Index")
    </p>
}
