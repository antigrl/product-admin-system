@model PAS.Models.VendorType

@{
    ViewBag.Title = "Archive Vendor Type";
}

<h2>Archive Vendor Type</h2>

<h3>Are you sure you want to Archive this Vendor Type?</h3>
<fieldset>
    <legend>VendorType</legend>

    <div class="display-label">
        @Html.DisplayNameFor(model => model.VendorTypeName)
    </div>
    <div class="display-field">
        @Html.DisplayFor(model => model.VendorTypeName)
    </div>
</fieldset>
@using(Html.BeginForm())
{
    @Html.AntiForgeryToken()
    <p>
        <input type="submit" value="Archive" />
        |
        @Html.ActionLink("Back to Vendor Type List", "Index")
    </p>
}
